# [workspace]
# authors = ["omar-abdelgawad <omar.abdelgawad@ejust.edu.eg>"]
# channels = ["conda-forge"]
# name = "information-sheet-problem"
# platforms = ["linux-64"]
# version = "0.1.0"

# [workspace] 
# channels = ["https://prefix.dev/conda-forge"]
# platforms = ["win-64", "linux-64", "osx-arm64", "osx-64"]
# preview = ["pixi-build"]
#
# [dependencies] 
# python_rich = { path = "." }
#
# [tasks] 
# start = "rich-example-main"

[package] 
name = "conda_information_sheet"
# version = "0.1.0"

[package.build] 
backend = { name = "pixi-build-python", version = "0.*" }
channels = ["https://prefix.dev/conda-forge"]

# The Python backend can automatically map PyPI dependencies from your pyproject.toml to their corresponding conda packages. This means you don't need to manually duplicate your dependencies in both pyproject.toml and pixi.toml.
[package.build.config]
ignore-pypi-mapping = false

# [package.host-dependencies] 
# hatchling = "==1.26.3"
#
# [package.run-dependencies] 
# rich = "13.9.*"
